# Args
* c - create
* v - verbose (list files being added)
* f - filename of archive
* x - extract
* z - gzip compression
* j - bzip2 compression
* t - list contents
* r - add file to archive (or append dir to archive; UNCOMPRESSED ONLY)

```bash
# Create archive
tar -cvf filename.tar /path/to/compress
```
