```java
Build package is like this:

Fullpackage/
├── Backend/
│   └── spring-boot-proj/
│       ├── src/
│       │   ├── main/
│       │       ├── java/                        // Spring Boot application source code
│       │       ├── resources/
│       │       │   ├── static/                  // Angular build files (for production)
│       │       │   │   ├── index.html           // You do not need to put them here unless
│       │       │   │   ├── main.js              // you are going to run everything from the
│       │       │   │   ├── runtime.js           // spring-boot application.
│       │       │   │   └── styles.css
│       │       │   ├── application.properties   // Backend configuration
│       │       │   └── templates/               // (Optional) Server-rendered templates
│       │       └── test/                        // Backend tests
│       └── pom.xml                              // Maven configuration for Spring Boot
├── Frontend/
    ├── src/                                     // Angular application source code
    ├── node_modules/                            // Node.js dependencies (generated by npm)
    ├── angular.json                             // Angular project configuration
    ├── package.json                             // Node.js dependencies and scripts
    └── tsconfig.json                            // TypeScript configuration




// Safe SQL
String username = "admin'; DROP TABLE users; --";  // Malicious input
String sql = "SELECT * FROM users WHERE username = ?";
Query query = entityManager.createNativeQuery(sql);
query.setParameter(1, username);  // Use parameterized query
List<Object[]> results = query.getResultList();

// Unsafe SQL
String username = "admin'; DROP TABLE users; --";  // Malicious input
String sql = "SELECT * FROM users WHERE username = '" + username + "'";
Query query = entityManager.createNativeQuery(sql);
List<Object[]> results = query.getResultList();
















